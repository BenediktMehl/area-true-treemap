{
  "name": "area-true-treemap-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo: area-true-treemap npm package + interactive demo",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "npm run build --workspace area-true-treemap",
    "build:demo": "npm run build && npm run build --workspace treemap-demo",
    "dev:demo": "npm run build && npm run dev --workspace treemap-demo",
    "test": "npm run test --workspace area-true-treemap",
    "typecheck": "npm run typecheck --workspace area-true-treemap"
  }
}
